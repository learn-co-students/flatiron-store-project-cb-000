<p> Status: <%= @cart.status %>
<% @cart.line_items.each do |line_item| %>
  <p><%= link_to line_item.item.title, item_path(line_item.item) %> | <%= '$%.2f' % (line_item.item.price.to_f/100) %> | Quantity: <%= line_item.quantity %> | <% if line_item.item.inventory < 5 %>
    <strong>There are only <%= line_item.item.inventory %> left in stock.</strong>
  <% end %> </p>
<% end %>

<p>Order total: <strong><%= '$%.2f' % (@cart.total.to_f/100) %></strong> </p>

<%= button_to 'Checkout', checkout_path(@cart) if @cart.status != "submitted" %>
