<tr style = "height: 40px ;">
  <td><%= link_to item.title, item_path(item.id) %></td>
  <td><%= number_to_currency(item.price) %></td>
  <td><%= button_to '', line_items_path(item_id: item), :style => "float: right;", :form => {:id => "add_to_cart_form"} %> </td>
</tr>
