<%= content_tag wrapper_tag do %>
<%= link_to item.title, item %> | <%= number_to_currency(item.price.to_f/100) %> | <%= button_to 'Add to Cart', line_items_path(item_id: item) if user_signed_in? %>
<% end %>
