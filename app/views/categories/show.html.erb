<h1><%= @category.title %></h1>

<ul>
<% @items.each do |item| %>
    <li><%= link_to item.title, item_path(item) %> - <%= '$%.2f' % (item.price.to_f/100) %> <%= button_to 'Add to Cart', line_items_path(item_id: item) if user_signed_in? %> </li>
<% end %>
</ul>
